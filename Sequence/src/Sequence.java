/**
 * Created by toni on 22/02/16.
 */
public class Sequence {
    public static void main(String[] args) {
        Sequence s = new Sequence(1);
        System.out.println(s.printline(5));
    }

    String printline(int n) {
        String resultat = String.format("%d", init) {
            for (int i = 1; i < n; i++) {
                resultat = nextLine(resultat);

            }
        }
    return resultat;
    }

    String nextLine() {
        String nextLine = "";
        int contador = 0;
        return nextLine;
    }
}
